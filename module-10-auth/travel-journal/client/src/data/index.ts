import { createPost, getPosts, getSinglePost } from './posts';
import { login, register, getMe, logout, refresh } from './auth';

export { createPost, getPosts, getSinglePost };
export { login, register, getMe, logout, refresh };
